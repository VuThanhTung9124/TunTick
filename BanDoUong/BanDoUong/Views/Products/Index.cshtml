<!doctype html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>ToCoToCo - Mẫu trang tĩnh (Phiên bản ảnh & hiệu ứng)</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
    /* Custom styles */
    .product-img { height: 220px; object-fit: cover; }
    .news-img { height: 140px; object-fit: cover; }

    /* Carousel */
    .carousel { position: relative; overflow: hidden; border-radius: 1rem; }
    .carousel-track { display: flex; transition: transform 0.8s cubic-bezier(.2,.8,.2,1); }
    .carousel-slide { min-width: 100%; box-sizing: border-box; position: relative; }
    .carousel-slide img { width: 100%; height: 380px; object-fit: cover; display: block; filter: brightness(.85); }

    /* Overlay text on hero */
    .hero-content { position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); z-index: 20; color: white; text-align: center; }
    .hero-cta { background: linear-gradient(90deg, rgba(34,197,94,1), rgba(16,185,129,1)); }

    /* Indicators & arrows */
    .carousel-indicators { position: absolute; left: 50%; transform: translateX(-50%); bottom: 12px; display:flex; gap:8px; z-index:30 }
    .indicator { width:10px; height:10px; background:rgba(255,255,255,0.5); border-radius:50%; cursor:pointer }
    .indicator.active { background: white }
    .carousel-arrow { position:absolute; top:50%; transform:translateY(-50%); z-index:30; background:rgba(0,0,0,0.35); color:white; padding:8px; border-radius:999px; cursor:pointer }
    .carousel-arrow.left { left:12px }
    .carousel-arrow.right { right:12px }

    /* subtle hover animation for product cards */
    .card-hover { transition: transform .25s ease, box-shadow .25s ease; }
    .card-hover:hover { transform: translateY(-6px); box-shadow: 0 12px 30px rgba(0,0,0,0.12) }

    /* fade-in when visible */
    .fade-in { opacity:0; transform: translateY(18px); transition: opacity .6s ease, transform .6s ease }
    .fade-in.visible { opacity:1; transform: translateY(0) }

     {
      .carousel-slide img { height: 220px }
      .hero-title { font-size: 28px }
    }
    </style>
</head>
<body class="antialiased text-gray-700 bg-gray-50">
    <!-- Header -->
    <header class="bg-white shadow-sm sticky top-0 z-40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center py-4">
                <a href="#" class="flex items-center gap-3">
                    <img src="https://cdn-icons-png.flaticon.com/512/2983/2983762.png" alt="Logo" class="h-10 w-10 object-contain" />
                    <span class="hidden sm:inline-block text-lg font-bold text-amber-600">Tunio</span>
                </a>

                <nav class="hidden md:flex items-center gap-6 text-sm font-medium text-gray-600">
                    <a href="#" class="hover:text-amber-600">Giới thiệu</a>
                    <a href="#products" class="hover:text-amber-600">Sản phẩm</a>
                    <a href="#news" class="hover:text-amber-600">Tin tức</a>
                    <a href="#stores" class="hover:text-amber-600">Cửa hàng</a>
                </nav>

                <div class="flex items-center gap-3">
                    <button class="p-2 rounded-full hover:bg-gray-100">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-600" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h18v18H3z" /></svg>
                    </button>
                </div>
            </div>
        </div>
    </header>

    <!-- Hero carousel -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-8">
        <div class="carousel fade-in" id="heroCarousel">
            <div class="carousel-track">
                <div class="carousel-slide">
                    <img src="~/Content/Images/banner1.jpg" alt="banner 1" loading="lazy">
                    <div class="hero-content">
                        <h2 class="hero-title text-4xl sm:text-5xl font-bold drop-shadow-lg">Ưu đãi hè: Combo thạch 59k</h2>
                        <p class="mt-3 max-w-2xl mx-auto">Ngọt mát từ trái cây tươi – hạn chế hóa chất, tăng hương vị tự nhiên.</p>
                        <div class="mt-5 flex justify-center gap-3">
                            <a href="#products" class="hero-cta inline-block px-6 py-3 rounded-full text-white font-semibold shadow">Mua ngay</a>
                            <a href="#news" class="inline-block px-6 py-3 rounded-full bg-white/90 text-gray-800 font-medium">Xem chi tiết</a>
                        </div>
                    </div>
                </div>

                <div class="carousel-slide">
                    <img src="~/Content/Images/banner2.png" alt="banner 2" loading="lazy">
                    <div class="hero-content">
                        <h2 class="hero-title text-4xl sm:text-5xl font-bold drop-shadow-lg">Trà sạch, vị đậm</h2>
                        <p class="mt-3 max-w-2xl mx-auto">Nguồn trà chọn lọc, quy trình ủ chuẩn giúp giữ trọn hương vị.</p>
                        <div class="mt-5">
                            <a href="#products" class="hero-cta inline-block px-6 py-3 rounded-full text-white font-semibold shadow">Khám phá</a>
                        </div>
                    </div>
                </div>

                <div class="carousel-slide">
                    <img src="~/Content/Images/banner3.png" alt="banner 3" loading="lazy">
                    <div class="hero-content">
                        <h2 class="hero-title text-4xl sm:text-5xl font-bold drop-shadow-lg">Sản phẩm mới: Hồng trà kem</h2>
                        <p class="mt-3 max-w-2xl mx-auto">Hương kem mịn màng, vị trà nồng nàn.</p>
                        <div class="mt-5">
                            <a href="#products" class="hero-cta inline-block px-6 py-3 rounded-full text-white font-semibold shadow">Dùng thử</a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="carousel-arrow left" id="prevBtn" aria-hidden="true">‹</div>
            <div class="carousel-arrow right" id="nextBtn" aria-hidden="true">›</div>
            <div class="carousel-indicators" id="indicators"></div>
        </div>
    </section>

    <br />
    <br />

    <!-- Product list -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 -mt-10">
        <section id="products" class="bg-white rounded-xl shadow p-6 fade-in mt-8">
            <div class="text-center mb-6">
                <p class="text-sm text-amber-600">ToCoToCo Menu</p>
                <h2 class="text-2xl font-bold">Sản phẩm nổi bật</h2>
            </div>
            <form>
                <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-6">
                    
                    @foreach (var item in Model)
                    {
                        <article class="bg-gray-50 border border-gray-100 rounded-lg overflow-hidden card-hover">
                            @{
                                string pathImage = "~/Image_Water/" + item.thumbnail;

                            }

                            
                            <img class="w-full product-img"
                                 src="@Url.Content(pathImage)"
                                 alt="@item.name"
                                 loading="lazy" />

                            <div class="p-4">
                                <h3 class="text-sm font-semibold">@item.name</h3>

                                <p class="text-xs text-gray-500 mt-1">
                                    @item.price.ToString("N0")₫
                                    @if (item.old_price > item.price)
                                    {
                                        <span class="line-through text-gray-300 text-[11px]">
                                            @item.old_price.ToString("N0")₫
                                        </span>
                                    }
                                </p>

                                <div class="mt-3 flex items-center justify-between">
                                    @Html.ActionLink("Chi tiết", "Details", new { id = item.product_id },
                                        new { @class = "text-amber-600 text-sm font-medium" })

                                    @Html.ActionLink("Thêm giỏ hàng", "GioHang", new { id = item.product_id },
                                        new { @class = "text-sm bg-amber-600 text-white px-3 py-1 rounded-md" })
                                </div>

                            </div>
                        </article>
                    }
                </div>
            </form>

            <div class="text-center mt-6">
                <a href="#" class="inline-block px-6 py-2 border border-amber-600 text-amber-600 rounded">Xem tất cả</a>
            </div>
        </section>

        <!-- Promo banner -->
        <section class="mt-8 fade-in">
            <div class="rounded-xl overflow-hidden shadow relative">
                <img src="https://images.unsplash.com/photo-1492724441997-5dc865305da7?auto=format&fit=crop&w=1600&q=80" alt="promo" class="w-full h-64 object-cover" loading="lazy" />
                <div class="absolute inset-0 bg-gradient-to-r from-green-800/40 to-transparent flex items-center">
                    <div class="p-6 text-white max-w-3xl">
                        <h3 class="text-2xl font-bold">A taste of nature — A cup of happiness</h3>
                        <p class="mt-2 text-sm">Thưởng thức hương vị tự nhiên, nguyên liệu tươi sạch — tận hưởng từng ngụm trà.</p>
                        <div class="mt-4">
                            <a href="#products" class="inline-block bg-amber-600 text-white px-4 py-2 rounded">Xem thêm</a>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- News and promotions -->
        <section id="news" class="mt-8 grid grid-cols-1 lg:grid-cols-3 gap-6 fade-in">
            <div class="lg:col-span-2 bg-white rounded-xl shadow p-6">
                <h4 class="text-lg font-bold mb-4">Khám phá ToCoToCo - Nhận ngay khuyến mãi</h4>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <article class="flex gap-4">
                        <img class="w-36 news-img rounded" src="https://images.unsplash.com/photo-1542444459-db0b6f7d7f10?auto=format&fit=crop&w=800&q=80" alt="promo" loading="lazy" />
                        <div>
                            <h5 class="font-semibold">Combo 2 hộp thạch 59k</h5>
                            <p class="text-sm text-gray-500 mt-1">Ưu đãi hấp dẫn cho khách hàng mới — thử ngay!</p>
                            <a href="#" class="text-amber-600 text-sm mt-2 inline-block">Xem thêm</a>
                        </div>
                    </article>

                    <article class="flex gap-4">
                        <img class="w-36 news-img rounded" src="https://images.unsplash.com/photo-1511920170033-f8396924c348?auto=format&fit=crop&w=800&q=80" alt="news" loading="lazy" />
                        <div>
                            <h5 class="font-semibold">Trà xanh Organic</h5>
                            <p class="text-sm text-gray-500 mt-1">Quy trình ủ trà đặc biệt giúp giữ mùi thơm tự nhiên.</p>
                            <a href="#" class="text-amber-600 text-sm mt-2 inline-block">Xem thêm</a>
                        </div>
                    </article>

                </div>
            </div>

            <aside class="bg-white rounded-xl shadow p-6">
                <h5 class="font-bold mb-4">Tin nổi bật</h5>
                <ul class="space-y-3 text-sm text-gray-600">
                    <li><a href="#" class="hover:text-amber-600">Vị tự hào - Tự hào Việt Nam</a></li>
                    <li><a href="#" class="hover:text-amber-600">Over Đậu - Sáng tạo mới</a></li>
                    <li><a href="#" class="hover:text-amber-600">Top 5 thương hiệu uy tín 2023</a></li>
                </ul>
            </aside>
        </section>

    </main>

    <!-- Footer -->
    <footer class="mt-12 bg-gradient-to-t from-stone-900 to-stone-800 text-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 grid grid-cols-1 md:grid-cols-3 gap-8">
            <div>
                <h6 class="font-bold text-lg">Công ty CP TM & DV ToCoToCo Việt Nam</h6>
                <p class="text-sm mt-3 text-gray-300">Địa chỉ: Tầng 7, Tòa nhà Times City, Hà Nội</p>
                <p class="text-sm text-gray-300 mt-1">Hotline: 1900 1234</p>
            </div>

            <div>
                <h6 class="font-bold text-lg">Về chúng tôi</h6>
                <ul class="mt-3 text-sm text-gray-300 space-y-2">
                    <li><a href="#" class="hover:text-white">Giới thiệu</a></li>
                    <li><a href="#" class="hover:text-white">Chính sách</a></li>
                    <li><a href="#" class="hover:text-white">Liên hệ</a></li>
                </ul>
            </div>

            <div>
                <h6 class="font-bold text-lg">Kết nối</h6>
                <p class="text-sm text-gray-300 mt-3">Đăng ký nhận tin khuyến mãi</p>

                <form class="mt-3 flex flex-col gap-2" action="/Emails/Create" method="post">
                    @Html.AntiForgeryToken()
                    <h4 class="text-green-400 mt-3">@ViewBag.ThongBaoEmail</h4>
                    <input type="email" name="DiaChiEmail" placeholder="Email của bạn"
                           class="w-full px-3 py-2 rounded bg-white/10 focus:outline-none" required />

                    <input type="text" name="NoiDung" placeholder="Nội dung bạn muốn hỏi..."
                           class="w-full px-3 py-2 rounded bg-white/10 focus:outline-none" required />

                    <button class="px-4 py-2 bg-amber-500 rounded font-semibold hover:bg-amber-600"
                            type="submit">
                        Gửi
                    </button>
                </form>

                
            </div>

        </div>

        <div class="border-t border-white/10 text-center py-4 text-sm text-gray-300">© 2025 ToCoToCo. Bản quyền thuộc về Công ty.</div>
    </footer>

    <script>
    // Carousel logic
    (function(){
      const track = document.querySelector('.carousel-track');
      const slides = Array.from(track.children);
      const prev = document.getElementById('prevBtn');
      const next = document.getElementById('nextBtn');
      const indicators = document.getElementById('indicators');
      let index = 0;
      let width = slides[0].getBoundingClientRect().width;

      function createIndicators(){
        slides.forEach((_, i) => {
          const dot = document.createElement('div');
          dot.className = 'indicator' + (i===0? ' active':'');
          dot.addEventListener('click', ()=> goTo(i));
          indicators.appendChild(dot);
        });
      }

      function update(){
        track.style.transform = `translateX(-${index * 100}%)`;
        Array.from(indicators.children).forEach((d,i)=> d.classList.toggle('active', i===index));
      }

      function nextSlide(){ index = (index + 1) % slides.length; update(); }
      function prevSlide(){ index = (index - 1 + slides.length) % slides.length; update(); }
      function goTo(i){ index = i; update(); }

      createIndicators();
      window.addEventListener('resize', ()=> width = slides[0].getBoundingClientRect().width);
      let timer = setInterval(nextSlide, 4500);
      document.getElementById('heroCarousel').addEventListener('mouseenter', ()=> clearInterval(timer));
      document.getElementById('heroCarousel').addEventListener('mouseleave', ()=> timer = setInterval(nextSlide, 4500));

      prev.addEventListener('click', prevSlide);
      next.addEventListener('click', nextSlide);

      // fade-in on scroll
      const observer = new IntersectionObserver(entries=>{
        entries.forEach(e=>{ if(e.isIntersecting) e.target.classList.add('visible'); });
      }, {threshold:0.18});
      document.querySelectorAll('.fade-in').forEach(el=> observer.observe(el));
    })();
    </script>
</body>
</html>
